# labshare
LabShare is a lightweight network hub for computer labs to exchange files over Wi-Fi without USBs or email. It features a stark dark interface inspired by terminal environments. Running a simple command opens a local web address where anyone can upload assignments, download templates, and clear workspace directories through one unified dashboard.
